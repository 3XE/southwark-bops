<!DOCTYPE html>
<html lang="en" class="govuk-template app-html-class">

<%= render partial: "/layouts/head" %>

<body class="govuk-template__body app-body-class">
  <script>
    document.body.className = ((document.body.className) ? document.body.className + ' js-enabled' : 'js-enabled');
  </script>

  <%= render partial: "/layouts/header" %>

  <%= link_to "Skip to main content", "#main-content", class: "govuk-skip-link" %>

  <div class="govuk-width-container">
    <%= render partial: "/layouts/alpha_phase_banner" %>

    <% if content_for?(:back_link) %>
      <%= link_to "Back", yield(:back_link), class: "govuk-back-link" %>
    <% end %>

    <main class="govuk-main-wrapper app-main-class" id="main-content" role="main">
      <% if content_for?(:page_title) %>
        <h1 class="govuk-heading-xl">
          <%= yield(:page_title) %>
        </h1>
      <% end %>

      <% if content_for?(:left_bar) %>
        <div class="govuk-grid-row">
          <div class="govuk-grid-column-one-half govuk-grid-column-one-third-from-desktop bops-left-bar">
            <%= yield(:left_bar) %>
          </div>

          <div class="govuk-grid-column-one-half govuk-grid-column-two-thirds-from-desktop">
            <%= yield %>
          </div>
        </div>
      <% else %>
        <%= yield %>
      <% end %>
    </main>
  </div>

  <%= render partial: "/layouts/footer" %>
  <script>window.GOVUKFrontend.initAll()</script>
</body>

</html>
